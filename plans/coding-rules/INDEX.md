# Coding Rules Index

Load this file at the start of every implement and review session. Then load only the rule files that apply to the current ticket's tech stack — do not load files that don't apply.

## Rule files

| File | Load when |
|---|---|
| [general.md](general.md) | Always |

## Repo-specific overrides

| File | Load when |
|---|---|

Repo-specific rules override general rules where they conflict.

## Adding rules

Only add a row here after you have created the file. A missing file is worse than a missing row.

Add rules to the relevant file as you discover them during implementation or review. Keep rules short — one clear sentence. New tech stack: create the file, then add a row above.
