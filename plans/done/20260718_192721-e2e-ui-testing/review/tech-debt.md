## Round 1 — 2026-07-18
- [DEBT] Like-count parsing `Number((await likeCount.textContent())?.match(/(\d+) likes/)?.[1])` is duplicated 3x across `apps/e2e/tests/like-flow.spec.ts` and `apps/e2e/tests/story-form-flow.spec.ts` — extract a `getLikeCount(page)` helper if a 4th usage appears. (Standards) — moved to plans/tech-debt/20260719_084347-e2e-like-count-parsing-duplicated.md
- [DEBT] `apps/e2e/tests/dark-mode-toggle.spec.ts` asserts 3 things (initial state, post-toggle, post-reload) in one test — acceptable as a single user journey, but flagged against the "one assertion per test" rule. (Standards) — moved to plans/tech-debt/20260719_084348-e2e-dark-mode-toggle-multi-assert.md
- [DEBT] `apps/e2e/tests/story-form-flow.spec.ts` both tests assert multiple things per test (form state + like count / error message + visibility + like count) — same "one assertion per test" judgment call as above. (Standards) — moved to plans/tech-debt/20260719_084349-e2e-story-form-multi-assert.md
- [DEBT] `apps/e2e/tests/smoke.spec.ts` is a 4th test file not called for by spec's "one seam per file, matching the 3 core flows" decision. Low-cost, harmless. (Spec) — moved to plans/tech-debt/20260719_084350-e2e-smoke-spec-out-of-scope.md
- [DEBT] `.github/workflows/e2e.yml` adds a `workflow_dispatch` trigger not mentioned in the spec (which only calls for `pull_request` and `push` to `main`). Harmless, enables manual runs. (Spec) — moved to plans/tech-debt/20260719_084351-e2e-workflow-dispatch-not-in-spec.md

## Round 2 — 2026-07-18
- [DEBT] `apps/e2e/playwright.config.ts` sets `E2E_STACK_WAS_ALREADY_RUNNING` as a top-level module side effect, read later by `apps/e2e/global-teardown.ts` in a separate invocation — an implicit, order-dependent global-state channel rather than an explicit passed value. (Standards) — moved to plans/tech-debt/20260719_084352-e2e-teardown-env-var-global-state.md
- [DEBT] `!process.env.CI` is checked twice in `apps/e2e/playwright.config.ts` — once gating the `isStackAlreadyRunning()` probe, once in `reuseExistingServer: !process.env.CI` — same "are we local" knowledge duplicated in two places. (Standards) — moved to plans/tech-debt/20260719_084353-e2e-ci-check-duplicated.md
- [DEBT] `isStackAlreadyRunning()` in `apps/e2e/playwright.config.ts` runs `execFileSync("docker", ["compose","ps",...])` with no try/catch; a raw child_process error surfaces if Docker isn't running, less helpful than `global-setup.ts`'s retry-with-context pattern for the same class of failure. (Standards) — moved to plans/tech-debt/20260719_084354-e2e-docker-probe-no-try-catch.md
- [DEBT] `isStackAlreadyRunning()` returns true if *any* container is running, not that the full `web`/`api`/`postgres` stack is up — a stale single leftover container (e.g. only `postgres` from a crash) makes teardown skip cleanup even though `docker compose up --build` started fresh containers. Spec's "reuse an already-running stack" implies the whole stack. (Spec) — moved to plans/tech-debt/20260719_084355-e2e-stack-probe-partial-match-false-positive.md
- [DEBT] `apps/e2e/playwright.config.ts` sets both `fullyParallel: true` and `workers: 1` — `workers: 1` already forces serial execution (needed for the shared, non-isolated DB), making `fullyParallel: true` dead/confusing config. (Spec) — moved to plans/tech-debt/20260719_084356-e2e-fullyparallel-workers-redundant.md
